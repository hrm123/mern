
type Query{
		orders: [Order]
	}
type Order{
		id: ID!
		items: [OrderItem]
		subtotal: Float!
		customer: Customer!
		orderDate: String!
	}
type OrderItem{
    product: Product!
    quantity: Int!
}
