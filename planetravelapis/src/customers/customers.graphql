
type Query{
	customers: [Customer]
}

type Customer{
    id: ID!
    name: String!
    email: String!
    orders: [Order]
}

type Mutation {
    registerCustomer(name: String!, email: String!, password: String!): Customer
}
